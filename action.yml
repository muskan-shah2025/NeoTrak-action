name: 'My Custom NeoTrak Action'
description: 'Example custom action that uses username and password'
inputs:
  username:
    required: true
  password:
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
